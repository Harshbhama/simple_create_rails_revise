
<div>
	<%= form_for @post, url: posts_new_path do |p| %>
		<%= p.label :title %>
		<%= p.text_area :title %>
		<br>
		<%= p.label :content %>
		<%= p.text_area :content %>
		<br>
		<%= p.submit %>
	<% end %>
</div>